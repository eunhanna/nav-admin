# nav-admin 开发约定

## 组件拆分

- 新建或重构页面前，先按“页面编排、数据/副作用、可复用展示、复杂交互表单或浮层”评估职责边界。
- 页面文件负责路由级编排与特定业务状态；独立、可复用或超过简单展示复杂度的表单、抽屉、弹窗、表格单元格应拆到 feature 下的组件文件中。
- 不把页面、复杂交互、表单字段和辅助逻辑无边界堆放在同一文件；拆分后保持 props 小而明确，副作用归属清晰。
- 抽取组件前优先复用现有 Ant Design 组件、全局样式和项目已有模式，不重复造轮子。
